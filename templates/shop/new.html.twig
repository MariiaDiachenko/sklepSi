{% extends 'base.html.twig' %}

{% block title %}{{ 'title.new_shop' | trans }}{% endblock %}

{% block body %}
    <h1>{{ 'header.new_shop' | trans }}</h1>

    {{ include('shop/_form.html.twig') }}

    <a href="{{ path('shop_index') }}">{{ 'button.back_to_list' | trans }}</a>
{% endblock %}
