{% extends 'base.html.twig' %}

{% block title %}{{ 'title.edit_shop' | trans }}{% endblock %}

{% block body %}
    <h1>{{ 'header.edit_shop' | trans }}</h1>

    {{ include('shop/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('shop_index') }}">{{ 'button.back_to_list' | trans }}</a>

    {{ include('shop/_delete_form.html.twig') }}
{% endblock %}
