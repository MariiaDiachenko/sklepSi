<div class="list-group">
  <h3>{{ 'header.categories' | trans }}</h3>
  {% for category in categories %}
    <a href="/product/?category={{ category.id }}" class="list-group-item">{{category.name}}</a>
  {% endfor %}
</div>
