{% extends 'base.html.twig' %}

{% block title %}{{ 'title.category_edit' | trans }}{% endblock %}

{% block body %}
    <h1>{{ 'header.category_edit' }}</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('category_index') }}">{{ 'button.back_to_list' | trans }}</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
